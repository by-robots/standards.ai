---
paths:
  - "**/*.{css,scss,sass,less}"
---

<!-- Adjust the paths above if this project keeps stylesheets somewhere unusual. -->

# CSS

- Follow the project's established CSS naming convention. If none exists, use [BEM](https://getbem.com/).
- Keep styles logically organised — one block per file or section. Do not mix unrelated blocks together.
